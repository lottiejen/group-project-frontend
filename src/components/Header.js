// Header component to be used across all the views

const Header = () => {
    return (
        <div className="mb-4 mt-4">
            <h1>App Title</h1>
        </div>
    );
};

export default Header;